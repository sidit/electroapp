export interface IProduct{
    id: number,
    stock : number,
    title: string,
    description: string,
    price: number,
    image: string,
    images: string,
    quantity: number,
    stotal: number;
}
